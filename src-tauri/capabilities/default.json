{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only what the tunnel UI needs: it talks to the core through our own commands, so no filesystem, shell or HTTP permission is granted.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default", "core:window:allow-start-dragging"]
}
