{
  "name": "nunya-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predev": "npm run sync-flags",
    "design": "vite --port 1421 --open /design/",
    "predesign": "npm run sync-flags",
    "build": "tsc --noEmit && vite build",
    "prebuild": "npm run sync-flags",
    "preview": "vite preview",
    "tauri": "tauri",
    "sync-flags": "node scripts/sync-flags.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "flag-icons": "^7.5.0",
    "jsqr": "1.4.0",
    "uqr": "0.1.3",
    "world-atlas": "2.0.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
